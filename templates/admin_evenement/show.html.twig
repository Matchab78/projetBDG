{% extends 'base.html.twig' %}

{% block title %}Evenement{% endblock %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/editadminpage.css') }}">
{% endblock %}
{% block body %}
    <h1>Evenement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evenement.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ evenement.description }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ evenement.ville }}</td>
            </tr>
            <tr>
                <th>Rue</th>
                <td>{{ evenement.rue }}</td>
            </tr>
            <tr>
                <th>Cp</th>
                <td>{{ evenement.cp }}</td>
            </tr>
            <tr>
                <th>DateEvent</th>
                <td>{{ evenement.dateEvent ? evenement.dateEvent|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Places</th>
                <td>{{ evenement.places }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_evenement_index') }}">back to list</a>

    <a href="{{ path('app_admin_evenement_edit', {'id': evenement.id}) }}">edit</a>

    {{ include('admin_evenement/_delete_form.html.twig') }}
{% endblock %}
